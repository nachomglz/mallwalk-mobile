import React from "react";
import { Text, View } from "react-native";

const Receipt  = () => {
    return <View>
        <Text>
            hola mundo (receipt)
        </Text>
    </View>
}

export default Receipt